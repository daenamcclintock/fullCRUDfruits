{% layout 'layout.liquid' %}
{% block content %}
    <div>
        <article>
            <h2>
                {{ fruit.name }}
                {% if fruit.readyToEat %}
                    Ready to Eat
                {% else %}
                    Not Ready to Eat
                {% endif %}
            </h2>
        </article>
    </div>
{% endblock %}